input StartChatInput {
    fromId: ID!
    toId: ID!
    listingId: ID!
}

input UniqueChatInput {
    id: ID!
}

input SendMessageInput {
    conversationId: ID!
    authorId: ID!
    message: String!
}
