"""
Represents a group in the system
"""
type Group {
  # Unique group ID
  id: ID!

  # Name of the group
  name: String!

  # Collection of members of the group
  members: [Idol!]!
}

type Query {
  groups(filter: GroupFilters, size: Int, pageNo: Int): [Group!]!
  faveGroups: [Group!]!
}
