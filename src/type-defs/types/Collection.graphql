"""
Represents a collection owned by a user
"""
type Collection {
  id: ID!
  title: String!
  description: String
  user: User!
  taggedGroups: [Group!]
  taggedIdols: [Idol!]
}

type CollectionFeed {
  page: Int!
  collections: [Collection!]!
}

type Query {
  collections(input: CollectionFilters!): [Collection!]!
  collectionsFeed(page: Int!): CollectionFeed!
}

type Mutation {
  insert_collections(input: [AddCollectionInput!]!): [Collection!]!
  update_collections(input: [UpdateCollectionInput!]!): [Collection!]!
  delete_collections(input: [UniqueCollectionInput!]!): [Collection!]!
}
